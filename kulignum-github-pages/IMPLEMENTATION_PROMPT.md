# Codex continuation prompt

Use this existing Astro project as the base implementation for kulignum.de. Finish by replacing placeholder SVG images in `public/images/` with optimized real photographs, then run `npm install`, `npm run build`, and fix any warnings. Keep Astro, TypeScript, static rendering, responsive CSS, DSGVO-friendly map placeholder, and route-compatible service URLs.
